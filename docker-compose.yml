version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./jobs.db:/app/jobs.db
    environment:
      - DOCKER_ENV=true
      - FLASK_ENV=development
